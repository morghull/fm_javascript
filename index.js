//let sideSquare = prompt('Enter side of square', '10');

const getPerimeterSquare = function (side) {
  if (isNaN(a)) {
    return null;
  }
  return 4 * side;
};

const isNumber = function (value) {
  if (
    value === '' ||
    value === null ||
    isNaN(Number(value))
  ) {
    return NaN;
  }
  return Number(value);
};

let sideSquare = 'qwe';
console.log(getPerimeterSquare(3));
let changeSidSquare = isNumber(sideSquare);
console.log(getPerimeterSquare(changeSidSquare));
